# 更新日志

## 版本方案

| 形式 | 含义 |
|---|---|
| `v0.x.y` | **现行官方序列。** 标注（annotated）标签。`v1.0.0` 的目标是「功能完整且经实机验证稳定」 |
| `pre-v1.z` | **重编号之前的历史标签**，轻量标签，仅作追溯用，不再新增 |

> 重编号发生在 2026-09-11。此前模组从 `1.0.0` 起编，但提交信息里的版本号与实际
> 代码版本并不一致（首个模组提交代码是 `1.0.0`，标签却打成 `v1.1`）。重编号
> 一并修正了这个问题。
>
> **Git 提交信息中的 `v1.x` 字样保留未改**（不改写历史）。对照关系见下表。

---

## 版本映射表

| 现行标签 | 历史标签 | 原代码版本 | 提交 | 主要内容 |
|---|---|---|---|---|
| `v0.1.0` | `v1.1` | 1.1.0 | `5e43d63` | 首个模组版本。朗读无配音剧情文本 / 手机消息 / 选项，数字键选择，重读键由 R 改为 Backspace |
| `v0.2.0` | `v1.3` | 1.3.0 | `3ee52a3` | 限时选择豁免（246 处 8 秒倒计时）+ 多后端语音层（Tolk / NVDA / SAPI） |
| `v0.3.0` | `v1.4` | 1.4.0 | `c8eec60` | 菜单 / 存档 / 设置界面的键盘导航与朗读 |
| `v0.4.0` | `v1.5` | 1.5.0 | `b392af3` | 修复纯键盘进入游戏必崩（UiNav 场景切换原生崩溃） |
| `v0.4.1` | `v1.6` | 1.6.0 | `2760af9` | 修复确认弹窗无法用键盘操作（UI 遮挡层级） |
| `v0.4.2` | `v1.7` | 1.7.0 | `83ff514` | 减少导航噪声（面板分组）、槽位标签拼接多字段、恢复视觉反馈 |
| **`v0.5.0`** | — | **0.5.0** | `0634df9` | **版本号转换版，功能与 `v0.4.2` 相同** |
| **`v0.5.1`** | — | **0.5.1** | `5760a22` | 修复回车/空格的按键归属：非导航模式下推进剧情不再误触控件 |
| **`v0.5.2`** | — | **0.5.2** | `60b7dab` | 限时选择：倒计时改回有限秒数，恢复「沉默」分支，新增沉默按键 |
| **`v0.5.3`** | — | **0.5.3** | `513ed81` | 重扫后选中项换了控件就重新播报（主菜单 START/EXIT 误触的根因） |
| **`v0.5.4`** | — | **0.5.4** | `2b7472b` | 标题画面 EXIT 加二次确认（匹配写错，**已作废**，见 v0.5.5） |
| **`v0.5.5`** | — | **0.5.5** | `5c88e7d` | 退出确认改成按实查对象名精确匹配（v0.5.4 正则会命中反的按钮） |
| **`v0.5.6`** | — | **0.5.6** | `8e48cc7` | 设置界面朗读用途：标签在控件外面，现在会往同一行和祖先里找 |
| **`v0.5.7`** | — | **0.5.7** | 见 `v0.5.7` 标签 | 修复沉默按键失效（`Enum.TryParse("0")` 解析成 `KeyCode.None`） |
| **`v0.5.8`** | — | **0.5.8** | 见 `v0.5.8` 标签 | 界面导航：按屏幕位置排序、排除画面外的控件、面板标题不再当按钮标签、激活后及时重扫；姓名输入框朗读「姓氏 / 名字」 |
| **`v0.5.8a`** | — | **0.5.8a** | 见 `v0.5.8a` 标签 | **修复系统语音（SAPI）兜底从未出声**：Mono 没实现 COM 后期绑定，改为纯 P/Invoke 直调 `ISpVoice`。新增「语音后端」配置项 |
| — | — | 1.0.0 / 1.2.0 | — | 未单独打标签。1.0.0 是 `5e43d63` 之前的工作版；1.2.0 的改动并入 `3ee52a3` |

---

## 修复：有配音的台词按退格重读不出声（尚未发布）

**问题**：在一句**有配音**的台词上按 `Backspace`，什么都不会发生。

**根因**：重读键念的是 `_lastSpoken`，而「有配音」那个分支里有一句

```csharp
Speech.Stop();
_lastSpoken = "";      // ← 把重读缓冲区一起清掉了
return;
```

`_lastSpoken` 身兼两职：既是 `Say()` 里 0.4 秒去重的哨兵，又是重读键的来源。
有配音的行故意不出声（避免 TTS 和角色语音打架），清哨兵本身是对的，
但它顺手把「能不能重读」也一起决定了 —— 结果是**语音错过了，退格也翻不回来**。

**改法**：把两个职责拆开。

| 变量 | 职责 |
|---|---|
| `_lastSpoken` | 只做 0.4 秒去重哨兵 |
| `_lastLine` | **重读缓冲区**，`Backspace` 念的就是它 |

- `Say()` 里记一次 `Remember(text)`（无配音台词、手机消息、选项朗读都照旧）
- **有配音的分支也 `Remember(Compose(...))`** —— 这正是这次要修的那一条
- 重读键改走 `RepeatLast()`；缓冲区为空时念「还没有朗读过内容。」，
  而不是像以前那样静默什么都不做（静默会让人以为按键坏了）

**没有改的**：有配音的台词仍然不会自动朗读，仍然会打断上一句读完一半的朗读。
这次只是让它在**你主动按退格时**能被念出来。

行为与《不/存在的你和我》那个仓库对齐 —— 那边一直是这么做的，
`Plugin.cs` 里的原话是「有配音的行也会记进来：语音错过了，退格重读文本正是玩家要的」。

构建 0 警告 0 错误；新增的用户可见字符串 `还没有朗读过内容。`
已确认存在于产出的 DLL 里（.NET 的字符串字面量在 UTF-16 的 `#US` 堆，
用 UTF-8 搜是搜不到的，别被这个误导）。

> **尚未实机验证。** 版本号与发布另定 —— 这一步动的是代码，
> 按仓库惯例得有版本号才能发。

---

## 致歉：补上对 可味玩KawayiPlay 的道歉

只改文档，没动代码，所以没有版本号。

署名做完了，但还有一件事一直没做：**我们从来没有为反编译这件事道过歉。**

做这个模组的时候，我们反编译了 `Assembly-CSharp.dll`、用 UnityPy 解包了
Addressables、把剧本文本提取了出来 —— 这一步没有事先问过可味玩KawayiPlay。
技术上它绕不开（这游戏把一部分界面文字画成了图片，选项按钮又按屏幕坐标摆放，
不反编译就找不到可以挂钩子的地方），但**"必要"不等于"可以不打招呼"**。

**先核对了才敢写。** 致歉里那句"提取内容从未进入版本库"是查过才落笔的：

```
git log --all --pretty=format: --name-only --diff-filter=A
  → 全历史（含 preview 分支）没有任何 extracted/ 或 decompiled/ 文件
```

改动：

| 位置 | 变化 |
|---|---|
| `README.md` | 「关于本作」之后新增「致 可味玩KawayiPlay」，放在「郑重警告」之前 |
| `mod\package\安装说明.txt` | 「关于本作」一段里补上道歉（这份是随包发给玩家的） |
| `mod\licenses\THIRD-PARTY-NOTICES.txt` | 「五、关于本作」补上道歉 |

措辞与《不/存在的你和我》那个仓库的致歉段对齐：先认下"没打招呼"，
再说清技术上的必要性，最后给补救与联系方式 —— 顺序反了就成了找借口。

顺带确认一句：这段道歉**不是**因为文档里有剧透。上一节已经查过，
`README.md` 与安装说明里没有剧透；道歉针对的是反编译与解包这个动作本身。

---

## 文档修订：把开发者从「免责声明」里请到「署名」里

只改文档，没动代码，所以没有版本号。

**先纠正我自己写错的一个判断。** 我原先在这里写的是"这份文档从头到尾没有出现过
开发者的名字" —— 这是错的。查了一遍改动前的版本库，**"可味玩 Kawayi Play"
其实出现过 4 次**：

| 位置 | 原文 | 性质 |
|---|---|---|
| `README.md` | 「非官方，与游戏开发商 **可味玩 Kawayi Play** 无关」 | **免责声明** |
| `mod\licenses\THIRD-PARTY-NOTICES.txt` | 「本模组非官方，与游戏开发商 可味玩 Kawayi Play 无关」 | **免责声明** |
| `README.md` | 存档路径 `LocalLow\Kawayi Play\TransparentHer\` | 顺带的字符串 |
| `mod\package\安装说明.txt` | 同上，存档路径 | 顺带的字符串 |

所以准确的说法不是"没提过"，而是：**提过 4 次，但没有一次是署名。**
两次出现在"我们和他们没关系"这句话里，两次只是文件路径里的一个目录名。
玩家读完整份 README，能得到的印象是"这东西和 KawayiPlay 无关"，
而不是"这游戏是 KawayiPlay 做的"。

（顺带：另一个仓库《不/存在的你和我》也是同一个毛病 —— 发行商 Nino Games 的名字
在那边的文档里出现过 4 次，**全都埋在存档路径 `LocalLow\Nino\…` 里**。）

这次改的就是这件事：**从"免责声明里带过的名字"变成"署名表里的主角"。**

名字除了凭记忆，还另做了一次搜索核对：

```
Steam 商店 API   https://store.steampowered.com/api/appdetails?appids=3021350
                 name        = 透明的她与真实的我
                 developers  = 可味玩KawayiPlay
                 publishers  = 可味玩KawayiPlay      ← 自研自发，同一家
                 release_date = 2025 年 9 月 22 日

游戏自己的元数据  <游戏目录>\TransparentHer_Data\app.info
                 Kawayi Play / TransparentHer        ← 厂商名对得上
```

改动：

| 位置 | 原来 | 现在 |
|---|---|---|
| `README.md` | 只说"与游戏开发商 **可味玩 KawayiPlay** 无关" | 新增「关于本作」署名表（中英文名 / 开发 / 发行 / AppID / 发售日）；点名开发兼发行 |
| `README.md` | "请尊重开发者的劳动成果" | **"这个补丁是让你能玩上这款游戏，不是让你不必买它"** |
| `README.md` | "如您是开发者或版权方，认为本仓库构成侵权" | 点名 **可味玩KawayiPlay**，并说明"任何不妥都请联系，我们会立即配合调整或移除" |
| `mod\package\安装说明.txt` | 正文没有任何署名与正版提示（厂商名只出现在存档路径字符串里） | 开头新增「关于本作」一段（这份是随包发给玩家的，更该有） |
| `mod\licenses\THIRD-PARTY-NOTICES.txt` | 结尾一句带过 | 新增「五、关于本作」，把署名、正版提示、联系方式写全 |

另外把公司名统一成 Steam 商店页上的写法 **可味玩KawayiPlay**（此前文档里写成
"可味玩 KawayiPlay"，中间多一个空格）。

顺带做了一次剧透排查：把 README 和安装说明扫了一遍"结局 / 真相 / 伏笔 / 真实身份"
这类词，命中的全是误报（`Home`/`End` 的"最后一项"、"钉死某个语音后端"）。
**这两份文档里没有剧透**，不需要动。

> 这一版**已发布**为 `v0.5.8a(Reset)`，见 Releases。发布包没有重新编译 ——
> 旧包里逐字节换掉 `安装说明.txt` 与 `licenses\THIRD-PARTY-NOTICES.txt` 两个文件，
> 其余 33 个文件（含 `TransparentHerA11y.dll`）与 `v0.5.8a` 完全相同，
> 详见下面的「发布 v0.5.8a(Reset)」。

---

## 发布 v0.5.8a(Reset)（2026-09-13）

只改文档的版本。发布包里 `TransparentHerA11y.dll` 的 MD5 与 `v0.5.8a` 逐字节相同，
**没有重新编译过分发用的那份二进制**，所以不需要重新实机验收。

### 顺带验了一次可复现构建，结论是它有效

打包时本来想"顺手确认一下重建产物和已发布的一致"，结果差点得出相反结论。

先拿 `F:\TransparentHerA11y-v0.5.8a\` 里那份 DLL 当基准，重建出来差了 84 字节，
看起来像"可复现构建其实没做到"。但那份 `F:\` 上的副本**不是已发布的那一份** ——
是从 GitHub Release 把 `TransparentHerA11y-v0.5.8a.zip` 下回来解压后才对上：

```
已发布 zip 里的 DLL   6F99A5B1C881F74510BC046BB7F34826
本地重建的 DLL        6F99A5B1C881F74510BC046BB7F34826   ← 逐字节相同
F:\ 上那份（旧副本）   B24589C9A5062DF5C32579DC2F30E427
```

所以 v0.5.8a 那次关掉 `EnableSourceLink` 的修法是有效的，
"任何人拿本仓库当前源码按 `build.ps1` 重建就能得到已发布的那一份"这句话成立。
教训是**基准要取发布产物本身，不能取手边任意一份副本**。

### 为什么是"换文件"而不是"重新打包"

这次唯一的改动是两份文档，二进制一个字节都不该动。所以做法是：
下载已发布的 zip，只替换那两份文件，其余原样保留 ——
这样"包里就是玩家验收过的那份二进制"是构造出来的，不依赖构建是否可复现。

替换后逐文件比对确认：35 个文件里只有那两份不同。

### 两个平台细节

- **GitHub 不接受附件名里的括号**。上传 `TransparentHerA11y-v0.5.8a(Reset).zip`
  被自动改成了 `TransparentHerA11y-v0.5.8a.Reset.zip`（括号变点号）。
  标签名不受影响，仍是 `v0.5.8a(Reset)`，URL 里百分号转义成 `%28Reset%29`。
- **PowerShell 5.1 的 `Invoke-RestMethod -Body` 传字符串时不发 UTF-8**：
  第一次建 Release，中文标题和正文全变成了 `?`。改成
  `-Body ([System.Text.Encoding]::UTF8.GetBytes($json))` 才正常。

---

## 发布：GitHub Release 整合包（2026-09-11）

v0.5.7 起改用 **GitHub Release 的 zip 附件**分发整合包：新玩家点一下下载、解压、
把里面的东西整体拷进游戏根目录即可，不必再自己拼 `BepInEx`。

附件名 `TransparentHerA11y-v0.5.7.zip`，内容就是 `mod\package\`（构建产物，不进版本库）。

### 包里新增 `licenses\`

模组代码是自己写的，但包里带了 **7 个第三方二进制**。此前包里一个许可证文件都没有 ——
BepInEx 官方发布的 zip 里也不带 LICENSE。而 LGPL-2.1 第 6 条要求：随二进制分发时
必须附上**完整许可证文本 + 显著声明 + 源码获取途径**。于是补上：

| 组件 | 版本 | 许可证 | 必须保留的版权行 |
|---|---|---|---|
| BepInEx | 5.4.23.5 | MIT | `Copyright (c) 2018 Bepis` |
| BepInEx.Harmony | 5.4.23.5 内置子模块 `d4cdcb4c` | MIT | `Copyright (c) 2019`（持有人上游本来就是空的） |
| HarmonyX | 2.9.0 | MIT | `Copyright (c) 2020 BepInEx` **和** `Copyright (c) 2017 Andreas Pardeike` |
| Mono.Cecil | 0.10.4 | MIT | `Copyright (c) 2008 - 2015 Jb Evain` **和** `Copyright (c) 2008 - 2011 Novell, Inc.` |
| MonoMod | 22.01.29.01 | MIT | `Copyright (c) 2015 - 2020 0x0ade` |
| UnityDoorstop | 4.5.0（`winhttp.dll`） | LGPL-2.1 | 无（FSF 原文，无逐文件声明） |
| NVDA Controller Client | Controller Client API 2.0 | LGPL-2.1 | 无（官方 `license.txt` 即 FSF 原文） |

许可证文本都是 2026-09-11 按上面的版本标签从上游仓库原样下载的，没有改写。
`THIRD-PARTY-NOTICES.txt` 里写明了 LGPL 组件的源码位置与索取方式
（两者都是未修改的独立 DLL，属 LGPL-2.1 第 6(b) 条的共享库情形）。

### 一个先入为主的错判：BepInEx 是 MIT，不是 LGPL

动手前我以为 BepInEx 5 是 LGPL-2.1 —— 这个印象挺常见，因为 **UnityDoorstop 是 LGPL**，
而两者总是一起出现。按版本标签逐个去上游取 LICENSE 之后才对上：

- BepInEx 本体是 **MIT**（`Copyright (c) 2018 Bepis`）
- 真正带 LGPL 的只有 **UnityDoorstop**（`winhttp.dll`）和 **NVDA Controller Client**
- 顺带两个坑：**Doorstop 3 是 CC0，Doorstop 4 起才改为 LGPLv2.1**，不能沿用旧判断；
  **HarmonyX 需要两个许可证文件** —— `LICENSE` 是 BepInEx 的，`LICENSE.Harmony`
  是 Harmony 原作者 Andreas Pardeike 的，只放前者是漏的

如果当时按印象写，就会把 MIT 组件错标成 LGPL，同时漏掉真正有源码义务的两个组件。

### 构建可复现：这次才真正验证，并且揪出一个真问题

以前说的「可复现」**没有验证过**。当时比对的四个位置（`bin\Release`、`package\`、
正式版目录、试玩版目录）其实都是**同一次构建的副本**，MD5 当然一样 —— 那证明的只是
「拷贝没出错」。

这次打包顺手做了真测试：连续两次 `dotnet build -c Release --no-incremental`，
**第一次就露馅**：重建出来的 DLL 与实机验收的那一份差 72 字节。

| | MD5 | 来源 |
|---|---|---|
| 实机验收份（也就是最终发布的这份） | `CA218DDA05900ACF463F3E67EDFC18BC` | 2026-09-11 18:03 构建，已部署到正式版与试玩版 |
| 第一次重建 | `443C5F04D6065606B9E928153E9D0608` | 同一份源码、同一套引用 |

差异只有 **72 字节**，分布在 5 个区段，全部落在身份字段上（COFF 时间戳、MVID、PDB GUID）。
两条独立证据说明两者行为相同：抽出全部可打印字符串对比是 **610 : 610，零差异**
（版本号、程序集属性、所有字符串常量都在内）；`ilspycmd` 反编译两份 DLL，
产出的 65245 字节 C# **逐字节相同**。

### 根因：`EnableSourceLink`

`Deterministic=true` 和 `IncludeSourceRevisionInInformationalVersion=false` 早就设了，
生成的 `AssemblyInformationalVersion` 里也确实只有 `0.5.7`、没有 commit 号。
但 MSBuild 有效属性里还藏着一条：

```
EnableSourceLink = true        ← SDK 默认值
```

Source Link 会把「当前 git 提交」写进 **PDB**，而 PDB 的内容哈希又会进 DLL 的
**debug 目录（PDB GUID）**和确定性 **MVID**。于是：

- 同一个 HEAD 内连续构建两次 → 哈希相同（看着像没问题）
- `git commit --allow-empty` 换一个 HEAD 再构建 → **哈希立刻变**

「提交内可复现、一提交就变」，等于没修好。

修法就是加一行 `<EnableSourceLink>false</EnableSourceLink>` ——
我们又不发布 PDB，Source Link 在这个项目里没有任何收益。

### 结果：关掉之后，重建产物与实机验收那份**逐字节相同**

关掉 Source Link 后重建，MD5 回到 `CA218DDA05900ACF463F3E67EDFC18BC` ——
正是 18:03 部署进两个游戏目录、玩家实际验收的那一份；再换一个 HEAD 重建，
依然是 `CA218DDA…`。

所以这次 zip 里的 DLL 同时满足两件事：**是实机验收过的那一份**，
也是**任何人拿本仓库当前源码按 `build.ps1` 重建就能得到的那一份**。

（顺带确认：DLL 里没有 `ghp_`、没有 `github.com`、没有 `raw.githubusercontent`；
PDB 里原本有 Source Link 写进去的 `raw.githubusercontent` 文档，但不含任何凭据，
关掉之后连它也没有了。二者都不随安装包分发。）

---

## v0.5.8a

**修复一个一直存在、但没人听见的 bug：系统语音（SAPI）兜底从来没出过声。**

### 是什么问题

语音后端按 Tolk → NVDA → SAPI 降级，SAPI 是最后一层保险，
「不装任何读屏软件也能出声」靠的就是它。但这一层在 Unity 的 Mono 里
从来没有成功过 —— 玩家的日志里只有这一行：

    SAPI 不可用: NotImplementedException: The method or operation is not implemented.

原因：原来的实现走的是 COM 后期绑定（`Type.GetTypeFromProgID("SAPI.SpVoice")`
+ `Type.InvokeMember`），**Mono 根本没有实现这套反射式 COM 绑定**，
一调用就抛 `NotImplementedException`，然后被 catch 掉、静默降级到「无后端」。

### 后果为什么严重

只装了争渡读屏的机器上（本项目的目标玩家之一），当时三级后端是这样：

| 后端 | 结果 |
|---|---|
| Tolk | 没有 Tolk.dll → 不可用 |
| NVDA | 没装 NVDA → 不可用 |
| SAPI | `NotImplementedException` → 不可用 |

**三级全灭，整局游戏一片安静** —— 而这正是「模组是不是没装好」最容易被误解成的现象。

### 怎么修的

新增 `Sapi.cs`，改成**纯 P/Invoke、完全不碰 COM 互操作**：
自己 `CoInitializeEx` + `CoCreateInstance` 拿到 `ISpVoice` 接口指针，
再从对象 vtable 里按槽位取出函数地址直接调用（和 `Zdsr.cs` 同一套路数）。

    vtable: 20 Speak / 29 GetRate / 30 SetVolume / 31 GetVolume

初始化时**先把语速和音量读出来写进日志**，这样 vtable 槽位万一取错会当场露馅，
而不是等到玩家需要朗读时才静默失败：

    语音后端: SAPI（没检测到读屏软件，用系统语音朗读；语速=0，音量=100）

`SPF_ASYNC` 保持置位（SAPI 默认同步朗读，会阻塞 Unity 主线程把游戏卡死）。

### 顺带

- 新增配置项 **`[朗读] 语音后端`**（默认「自动」）。填 Tolk / NVDA / SAPI
  可以把它钉死在某一个上，用来排查「到底是哪个后端不响」；
- 后端全部不可用时，日志从「三个都不可用」升级成**逐个列出各自的失败原因**：
  `没有可用的语音后端。逐个结论：Tolk：…；NVDA：…；SAPI：…；`
- `Speech.Shutdown()` 现在会释放 SpVoice 并 `CoUninitialize`。

### 发版当天补的一个跟头（值得记下来）

第一版发布时，`BepInPlugin` 的版本号我按发布名写成了 `"0.5.8a"`。
结果玩家那边：**插件一行都没加载**，日志只有一句

    [Warning: BepInEx] Skipping type [TransparentHerA11y.Plugin] because its version is invalid.
    [Info   : BepInEx] 0 plugins to load

原因：`BepInPlugin` 的版本号字段**不是给人看的显示名**，它必须能被解析成
版本号（`System.Version.TryParse`）。带字母的写法会直接被判定非法，
BepInEx 随即**静默跳过整个插件** —— 游戏里一片安静，和「没装模组」一模一样，
极难往版本号上想。

修法：特性里改用 `"0.5.8.1"`（第四位数字承载 a 这次修订），发布名仍是 v0.5.8a。
并在 `build.ps1` 里加了**发布前守卫**：版本号必须是合法版本号、且与 csproj 一致，
否则直接中断构建 —— 这类错误以后在本地就会被拦下。

### 说明

这一版的改动是**从 v0.6.x 预览线里单独摘出来的 SAPI 修复**。
v0.6.x 那条线（争渡读屏直连）已经确认走不通 —— 争渡的语音接口对调用程序
有授权要求，实机在权限对等的前提下依然返回「没有运行或没有授权」（返回码 2），
客户端无法绕过。所以那部分代码不进稳定线。

---
## v0.5.8

**界面导航的四类问题：顺序和画面对不上、画面外的控件混进导航、面板标题被当成按钮标签、
面板变了不及时重扫。** 外加姓名输入框的朗读。

### 1. 编号与画面对不上

实测反馈：

> 编号（形如 1/12、2/13）往往与屏幕坐标是不对称或者说不对应的，有时候上面的按钮
> 或控件反而是 2/x，下面的是 1/x。

`SortWithin` 原来**先按兄弟序号路径排**，屏幕位置只是同级时的次要条件。可路径表达的是
「谁后渲染、谁在上层」，跟画面上看到的上下左右没有必然关系。

改成**位置优先**：先上后下、同一行先左后右，位置完全重合才退回渲染层级。

行号用「量化到 4 像素的格子」算，而不是「差值小于阈值就算同一行」—— 后者会让比较函数
**不可传递**（a≈b、b≈c 但 a 与 c 分属两行），`List.Sort` 会抛「比较函数不一致」，
而这里被 try 兜住后会把整组清空、导航直接退出。量化不存在这个问题。

### 2. 画面外的控件混进导航

实测反馈：

> 界面是这样的（标题画面），但我按上下光标在选存档，关键是选中按回车还真的能弹出确认框。

从 `level1` 资源里查到的结构：

```
Canvas/Panel/
  StartButton        anchoredPosition.x = -2713
  ExitButton         anchoredPosition.x = -2713
  Phone/Image/Buttons/Start Exit Load Save      全部 active = true
```

手机面板整块停在画面外，但那 4 个按钮是 `active` 的 —— 而 **Unity 的按钮根本不关心自己
有没有被画出来**，`onClick` 照样执行。模组此前只判断 `isActiveAndEnabled`，于是把它们
收进了导航：屏幕上是 START/EXIT，方向键却在走「读档 / 存档」，按回车真的会弹确认框。

新增 `VisiblyClickable`，只收**真的在画面上、真的点得到**的控件：

- 矩形和 Canvas 完全不相交 → 排除
- 从控件中心打一条 UI 射线打不到它自己 → 排除
- 判不出来一律**不排除**（少一个控件是功能缺失，多一个只是噪声）

射线只认「命中的是自己或自己的子物体」，**故意不认祖先** —— 整屏背景是所有控件的共同
祖先，认了它这道判断就失效。

### 3. 面板标题被当成按钮标签

实测反馈：

> 刚开始进入游戏大部分按钮都朗读为「通讯」。

「通讯」是手机面板顶部 `/Canvas/Panel/Phone/Image/TeleText` 上的一行 TMP。v0.5.6 的
「同行找标签」会从父级往上找两层：按钮的第一层父级 `Buttons` 没文字，第二层 `Image`
里有「通讯」，于是**面板里每个按钮都拿它当标签**。

改成按容器判断：**容器里除了这个控件还有别的 Selectable，它就是面板而不是「一行」**，
里面的文字（标题）不能当标签。

差点写错的判据：一开始想用「竖直对齐」，静态验算发现会误伤设置面板 —— 音量页的滑条行里
`Text` 与 `Slider` 是**上下堆叠**的（相差 66.5 画布单位，行高 48），过不了 1.2 倍阈值，
标签会全部丢失。当场撤掉换成容器判据，没有发出去测。

### 4. 面板变了不及时重扫

实测反馈：

> 我尝试点击了编号为 1/7 的按钮，没有提示界面更新，然后手机也就是二级菜单中的控件
> 可以朗读和导航了。

两个原因：

- 原来只在少数分支里请求重扫，而游戏自己换面板**不会**通知模组
- 播报条件是「当前项变了」—— 打开子菜单时当前那一项往往还是原来那个按钮、序号也没变，
  只比当前项就一声不吭

现在：每次激活控件后**无条件排两次重扫**（下一帧一次，14 帧后一次，照顾滑入/淡入动画），
并且播报条件扩展为「整份清单变了」。清单没变就不播报，多扫一次没有副作用。

### 还做了

- **姓名输入框**：`level2` 的两个输入框在场景里没有任何标签文字，对象名一个叫
  `FirstText（InputField）`、一个叫 `Third`（完全看不出是「名」）。依据游戏自己的
  `NameInputManagerTMP`（`surnameField1` / `nameField1`，Unity 按声明顺序序列化，
  解析组件字节确认两个字段槽分别指向这两个对象）朗读「姓氏」「名字」，并补上
  「当前为空」「最多 2 个字」
- **图片按钮的中文名**：`Yes / No / Confirm / Cancel / Save / Load / Exit / Skip / Start`
  的文字是**画在图片上的**（扫 level1–level5 全部 MonoBehaviour，这些对象里只有按钮状态名
  和回调名）。中文名取自游戏自己的本地化表（`alert.confirm` = 确定、`alert.cancel` = 取消、
  `bookmark.save.button` = 存档、`bookmark.load.button` = 读档），回调名也吻合
  （姓名确认框 `Yes → OnConfirm`、`No → OnCancel`）。别名只在控件自己子树没有文字时才生效
- **三个排查用配置项**：`按屏幕位置排序控件`、`只导航看得见的控件`、`界面诊断日志`
  （最后一个会把每一项的朗读文本、屏幕行列、层级路径，以及「有但没进导航」的控件
  全部写进日志）

### 实机验收（试玩版 + NVDA）

玩家实测结论：**没有恶性 BUG，体验可以**。已确认修好：编号与画面一致、画面外控件不再
进入导航、手机面板按钮不再全念「通讯」、激活后能及时听到「界面已更新」、姓名输入框能
分辨「姓氏 / 名字」。

已知遗留，**暂不处理**：**非顶层控件仍会被导航** —— 一个控件套在另一个控件里面时，
两者都会出现在导航列表里。低视力玩家更希望只走顶层控件。

---

## v0.5.7

**沉默按键按了没反应。**

问卷实测：

> 限时选择里按数字键 0 —— 按了没反应。

### 原因：`Enum.TryParse("0")` 会成功，但解析成的是 `KeyCode.None`

```csharp
Enum.TryParse("0", true, out parsed);   // 返回 true
// parsed 是数值 0 —— 而 KeyCode 里数值 0 就是 KeyCode.None，不是 Alpha0
```

配置项 `沉默按键` 的默认值正是字符串 `"0"`，于是：

- `_silenceKey` 被赋成 `KeyCode.None`；
- 判定里的 `sk != KeyCode.None` 直接为假 → 永远不触发；
- 连那句「按 0 可以立刻沉默」的提示也因为同一个判断没念出来。

`Enum.TryParse` 对纯数字串是按**数值**解析的，这是它和「枚举成员名」的区别，
而 `KeyCode.None = 0`、`KeyCode.Alpha0 = 48` 正好把这个坑藏得很深。

### 改法

新增 `Reader.TryParseKey`：

- **单个数字 `0`-`9` 先自己映射**到 `KeyCode.AlphaN`，同时给出小键盘上的对应键
  （和数字键选选项的行为一致：主键盘、小键盘都认）
- 解析结果等于 `KeyCode.None` 一律当没填
- 重读按键也换用同一个解析器，免得以后有人填「0」踩同一个坑

配置说明同步更新：数字 `0`-`9` 主键盘小键盘都认，也可以填 `Z`、`F1` 这类名称。

---


## v0.5.6

**设置界面只念得出控件类型，念不出用途。**

实测反馈：

> 设置的无障碍体验非常糟糕，只给控件类型，不给用途。

### 原因：标签是控件的**兄弟**，不是子节点

把设置面板的层级从 `level1` 里挖出来（UnityPy 解析 `Transform` / `RectTransform`
的 `m_Father` / `m_Children`）：

```
TextSpeed        [行]
  Text            ← 中文「文本显示速度」，是控件的兄弟
  Slider          ← 控件，子树里只有 Background / Fill Area / Handle，一个字都没有
ScreenPixel1080  [单选项]
  Text            ← 「1280×720」
  Toggle          ← 控件，子树里只有 Background / Checkmark
```

而 `TextOf` 只查控件**自己的子树**，查不到就退回对象名 —— 于是念出来是
「Slider，滑条，50 / 100」「Toggle，开关，开」。跟读屏无关，是这个结构没考虑到。

### 改法：三层查找

1. **自己子树里的文本** —— 有就用（存档槽、按钮等绝大多数控件走这条，行为不变）
2. **同一行的兄弟文本** —— 从自己往上找，最多两层；取「不属于自己子树、也不属于
   别的 Selectable」的第一段文本，并在前面补上所属行名
3. **都没有** —— 从自己往上找第一个有意义的名字，跳过 `Slider` / `Toggle` /
   `ControlButton` 这类样板名

排除「属于别的 Selectable 的文本」这条很关键：一行里往往还有别的按钮，
它们的文字不是这一行的标签。

### 行名和页签名是美术字，只能按对象名映射

从 `level1` 里直接抽中文字符串，能读到的只有**选项**文字
（`窗口模式` `全屏模式` `仅快进已读文本` `快进全文` `5秒` `15秒` `关闭`）；
`文本显示速度`、`画面模式`、`窗口分辨率`、`通用`、`音量` 这些**一个都不在场景里**：

- `文本显示速度` 这类是运行期从本地化表填进 TMP 的 → 运行期读得到
- `画面模式`、`窗口分辨率`、`快进模式`、`指针隐藏`、`画面位于最前`，以及三个页签名，
  **是画成图片的** → 读屏永远拿不到，只能退回对象名

所以加了一张小映射表（`UiNav.NameAlias`），每条都有依据、写在注释里：

| 对象名 | 中文 | 依据 |
|---|---|---|
| `General` / `Volume` / `Shortcut` | 通用 / 音量 / 快捷键 | 本地化表 `config.tab.*` |
| `ScreenPixelOption` | 窗口分辨率 | 选项是 1280×720 / 1600×900 / 1920×1080 |
| `FullScreenOption` | 画面模式 | 选项是 窗口模式 / 全屏模式 |
| `FastForwordOption` | 快进模式 | 选项是 仅快进已读文本 / 快进全文 |
| `HideCursorOption` | 指针隐藏 | 选项是 5秒 / 15秒 / 关闭 |
| `TopWindowOption` | 画面位于最前 | 选项是 ON / OFF |
| `CharacterVolumePanel` | 角色音量 | 本地化表 `config.title.charactervolume` |

于是设置界面读出来是：

```
通用，按钮，1 / 3
画面模式，全屏模式，开关，开
窗口分辨率，1920×1080，开关，开
文本显示速度，滑条，60%
```

### 顺带

- **滑条值改念百分比**：音量 / 速度条的取值范围是 0–1，原来念「1 / 1」等于没信息，
  现在念「100%」。0–1 以外的范围仍念「N / M」。
- 标签查找同时认 TMP 和旧版 `UnityEngine.UI.Text`：设置面板那些 `Text` 节点的静态文字
  在场景里是空的，无法确认用的是哪一种，两种都认最省事。

---


## v0.5.5

**v0.5.4 的退出确认打在了错的按钮上，完全反了。**

实测反馈：

> 有系统确认弹窗的你加了确认，但是系统没有弹窗确认的你和系统一样没有做确认。

### 原因：正则的词边界

v0.5.4 用 `\b(exit|quit)\b` 去匹配「对象名 + 标签」：

- **该命中的没命中**：标题画面的按钮对象名是 `ExitButton`。`Exit` 后面紧跟字母 `B`，
  两边都是 `\w`，**没有词边界**，所以 `\bexit\b` 匹配失败。
- **不该命中的命中了**：剧情场景里有个按钮对象名就叫 `Exit`（手机菜单的「退出游戏」，
  游戏自己会弹原生确认框），它反而匹配成功。

### 改法：按实查到的对象名精确比对

不再猜、也不再做包含匹配，直接把场景资源里的对象名挖出来（在
`TransparentHer_Data\level*.assets` 里按 ASCII 串搜索）：

| 场景 | 文件 | 出现的相关名字 |
|---|---|---|
| 标题画面 | `level1` | `StartButton` `ExitButton` `Title` `QuitGame` |
| 剧情画面 | `level3`-`level5` | `MenuButton` `QuitGame` `Exit` … **没有 `ExitButton`** |

完整版和试玩版结果一致。所以判定条件就是 **对象名 == `ExitButton`**，
只命中标题画面那一个按钮，剧情里那些自带确认框的按钮一个都不碰。

同时把名字提成配置项 `退出确认对象名`（默认 `ExitButton`，可逗号分隔多个，留空即关闭），
并在触发确认时写一行日志：

```
[UiNav] 退出确认：「<对象名>」标签「<标签>」场景 <场景名>
```

万一以后游戏更新改了名字，照着日志改配置即可，不用重新编译。

### 教训

这种「找到某个特定控件」的判定，**别用词边界，也别用包含匹配** ——
驼峰拼接的 `ExitButton` / `QuitGame` 会同时坑掉两种写法。直接拿实查到的名字全等比较。

---


## v0.5.4

**标题画面的 `EXIT` 加一道二次确认。**

标题画面只有两个同类按钮，美术字分别是 `START` 和 `EXIT`，只差一个单词。而区分它们真正靠的是
「哪一个是整块大面板、哪一个是角落小图标」—— 这种视觉信息读屏拿不到。按错一次，整个会话直接没了。

游戏**只在标题画面这一处**不给确认框。剧情中的「返回标题」和手机菜单的「退出游戏」游戏自己都会弹
「确认要返回标题吗？」这类原生确认框，所以那些地方不重复询问。

### 行为

导航模式下激活标签为英文 `exit` / `quit` 的控件时：

```
这是退出游戏。再按一次回车或空格确认退出，按别的键取消。
```

- 再按一次回车 / 空格 → 真的退出
- 按方向键 / `Tab` / 8 秒无操作 → 取消

### 为什么用标签匹配而不是写死对象名

`TextOf` 优先取 TMP 文本，取不到才回退 GameObject 名；而游戏里所有会退出的中文按钮标签都是中文。
所以 `exit` / `quit` 这两个英文关键字实际上只会命中标题画面那一个按钮。

新增配置项 `退出前二次确认`（默认开）。

---

## v0.5.3

### 由一次误触查出来的缺陷

实测反馈：在主菜单「按键盘操作某个控件，游戏就退出了，像是崩溃」。

先排除了崩溃 —— `Player.log` 结尾是 `DialogueSceneManager.OnDestroy()` + Unity 内存统计，
`BepInEx\LogOutput.log` 无异常，**Windows 应用程序错误日志里根本没有 `TransparentHer.exe`**。
这是 `Application.Quit()` 的正常收尾。实际按到的是标题画面的 `EXIT` 按钮，和 `START` 听混了。

但顺着这次误触，发现了导航模式自己的一个真缺陷：

**激活控件后的下一帧会重扫界面，而 `_index` 保留原序号，重扫后那个位置上可能已经换了
别的控件 —— 却不重新播报。**

```csharp
_pendingRescanFrame = Time.frameCount + 1;   // 激活按钮后
...
Scan();                                       // 列表重建、重新排序
_index = Mathf.Clamp(_index, 0, Items.Count - 1);   // 序号留着，控件可能换了
                                                  // ← 这里什么都没说
```

弹窗、二级菜单、翻页都属于这种情况。标题画面按下 `START` 会弹出「手机」二级菜单，
列表整个换掉，而导航光标还停在原序号上。玩家这时按回车，激活的是**他从没听过的东西**。
标题画面那两个按钮偏偏只差一个单词（`START` / `EXIT`），听错一次的代价就是直接退出游戏。

### 改法

重扫后比对「上一次播报过的控件」和「当前位置的控件」，换了就播报 `界面已更新。` 加新控件。
没换则一声不响（开关、滑条这类不改变列表的操作不会多嘴）。

---


## v0.5.2

**限时选择（「实时」）的倒计时被取消了，等于删掉了游戏的一条剧情分支。**

### 现象

限时选择里倒计时进度条走完了，剧情却停着不动；而且**再也拿不到「沉默」这个选项** ——
也就是什么都不做、等所有选项消失的那条路。

（实测反馈：`UI 没有被时停，但是剧情确实被时停了，导致玩家失去了沉默这个选项`。）

### 原因

把「实时」倒计时当成纯计时器了。它其实是一条**剧情分支**：

```csharp
// DialogueSceneManager.ExecuteRealTimeLogic
autoDestroyCoroutine = StartCoroutine(AutoDestroyAfterTime(8f, list[list.Count - 1]));

// DialogueSceneManager.AutoDestroyAfterTime
yield return new WaitForSeconds(delay);
ClearAllReplyButtons();
OnReplyButtonClicked(defaultScene.ToNumber);   // ← 这一批最后一条 = 沉默分支
```

`defaultScene` 是这一批的**最后一条**，它的 `ReplyStyle` 是 `"0.0"`，所以根本不会生成按钮
（日志里那句 `无效的 ReplyStyle 索引或索引超出范围：0.0` 就是它）。也就是说沉默在游戏里
本来就是「看不见的选项」，靠的就是倒计时到点。

而 `v0.2.0` 起把 `delay` 改成了 `86400f`（24 小时 ≈ 永不超时），这条分支于是永远不触发。

「UI 没有被时停」也是同一处设计漏算：进度条 `FadingSlider.FadeSliderOverTime` 的时长是
**写死的 8 秒**，跟被拉长的 `delay` 是两套时间。

### 改法

倒计时只能被**拉长**，不能被取消：

1. `限时选择时长`（新配置，默认 **20 秒**，范围 1–600）。填 `8` 即完全恢复游戏原版节奏。
   到点后照旧走沉默分支 —— 沉默回来了。
2. 补丁 `FadingSlider.FadeSliderOverTime`，把进度条时长改成同一个值，消除两套时间。
   （`FadingSlider` 全游戏只用在限时选择的计时条上。）
3. `沉默按键`（新配置，默认 `0`）。立刻走沉默分支，不必干等倒计时。
   和原版超时走的是同一条路径 `OnReplyButtonClicked(defaultScene.ToNumber)`。
   选项是 `1`-`9`，所以 `0` 不冲突。
4. 朗读选项时补一句：`这是限时选择：N 秒内不选，就算作沉默。按 0 可以立刻沉默。`
   —— 沉默在原版是看不见的选项，不说玩家不会知道它存在。

**已知不一致**：游戏自带的倒计时**音效**固定 8 秒，没办法跟着变长（是音频资源本身的长度）。
进度条已经跟上了，音效没有。要完全对齐就把时长设成 8。

### 不兼容变更

`限时选择不自动选`（bool）已被 `限时选择时长`（float）取代。旧配置项会变成无效残留，
不影响运行，介意就删掉那一行。

### 另外

- 补丁数 22 → 23。
- 导航模式激活控件时新增一行日志（`[UiNav] 激活 <类型>「<标签>」场景 <名>`）。
  起因是收到一次「键盘操作控件导致崩溃」的报告，但 `Player.log`、`BepInEx\LogOutput.log`、
  Windows 应用程序错误日志里**都没有任何异常或崩溃记录**，属于原生级终止，无从定位。
  有了这行日志，下次就能看出最后碰的是哪个控件。

---

## v0.5.1

**回车 / 空格的按键归属**（一次实测发现的交互 bug）。

### 现象

非导航模式下按回车/空格推进剧情时，会**同时**触发界面上某个控件。

### 原因

`UiNav` 在导航模式里用 `EventSystem.SetSelectedGameObject` 让游戏自己的按钮高亮态生效，
退出时只还原了 `sendNavigationEvents`，**没有清掉选中态**。而 uGUI 的
`StandaloneInputModule` 一见回车/空格就向「当前选中对象」发 `submit`，于是：

- 退出导航模式后残留的选中项 → 被再点一次；
- 鼠标点过的按钮（`Selectable.OnPointerDown` 会自动选中自己）→ 被再点一次；
- 游戏自己的 `AutoFocusInputField` 选中过的输入框同理。

同一个按键于是做了两件事：推进剧情 **+** 点击控件。

### 改法

把「按键归属」显式写成状态机，判定点是**导航模式是否有效且选中项还活着**，
而不是「EventSystem 里有没有选中对象」（后者会被鼠标和游戏自身干扰）：

| 状态 | 回车 / 空格 |
|---|---|
| 非导航模式 | 不归我们，交回游戏推进剧情 |
| 导航模式 + 当前没有可用控件 | 退出导航，交回游戏推进剧情 |
| 导航模式 + 当前有可用控件 | 归我们，激活该控件 |

配套两处改动：

1. **常驻关闭 uGUI 的提交通路**（`UiNav.KeepUnitySubmitOff`，由 `Plugin.Update`
   无条件调用，不受「菜单键盘导航」开关影响）。这条通路必须一直关着，否则鼠标点过的
   按钮仍会在按空格推进剧情时被重复点击。
   安全性依据：全游戏代码**没有任何一处** `SetSelectedGameObject`，游戏本就不依赖
   EventSystem 选中态；剧情推进、输入框确认、`Esc` 返回全部是自己读
   `Input.GetKeyDown`；鼠标点按走 pointer 事件不受该开关影响；游戏自己播视频时
   也用同一个开关关掉提交（`VideoPanelManager`）。
   代价：失去 uGUI 原生的方向键导航 —— 那正是导航模式要替代的东西。

2. **拦住游戏同一帧那次多余的推进**（补丁 `DialogueButtonClicked`，两个管理器各一份）。
   游戏是在 `Input.GetKeyDown(空格/回车)` 成立后紧接着调用它，所以只要那次按键已经
   归我们，这次调用就该跳过。两种判据都写上了（本帧是否已接管 / 按键是否正按下且
   有可激活项），因此不依赖两个 `Update` 谁先执行 —— 也放行我们自己激活控件时引发的
   推进（例如全屏热区按钮）。补丁数 20 → 22。

### 顺带修正

- 进入输入框时的顺序反了：原来先设焦点再退出导航，而退出会清掉高亮，等于把刚设的
  焦点又抹掉。现在先退出、再设焦点。
- 导航模式退出时主动清除自己设的选中态，高亮不再残留。

---

## v0.5.0 — 2026-09-11

**首次按新序列发布的 0.x 官方版。** 功能与 `0.4.2` 相同，本版为版本号转换版本。

内容即此前积累的全部功能与修复：

### 朗读
- 无配音剧情文本（旁白、主角「我」等），有配音的台词只放语音并打断上一句朗读
- 手机（WeSay）聊天消息，含说话人；表情消息提示为「图片」
- 选项朗读（剧情选项 + 手机选项）

### 交互
- 数字键 `1`-`9` 选择选项
- **限时选择豁免**：246 处「实时」限时选择不再 8 秒自动代选
- 菜单 / 存档 / 设置 / 画廊的键盘导航：`Tab` 进入，`↑↓` 切换，`←→` 调滑条，`回车/空格` 激活，`PageUp/PageDown` 切面板组

### 语音后端
按可用性自动挑选：**Tolk**（有则用，支持 NVDA / JAWS / SuperNova / ZoomText）→ **NVDA** → **SAPI**（无读屏也能出声）

### 稳定性修复
1. **原生崩溃**：激活按钮后 0.35 秒的定时界面重扫，恰好落在场景销毁/激活瞬间，对正在销毁的 `Selectable` 调用 `FindObjectsOfTypeAll` 触发原生崩溃。现象是鼠标进入正常、纯键盘进入必崩。现改为仅「下一帧」重扫并加场景稳定门禁。
2. **确认弹窗不可达**：弹窗是覆盖层，背后按钮仍 active，只按屏幕坐标排序会让读屏用户在底层按钮里打转。现按渲染路径排序。
3. **槽位无法区分**：存档槽有名字/时间/章节三个文本字段，只取第一个会让 5 个空槽都读成「无存档」。现拼接多字段。
4. **`StartCoroutine(null)` 隐患**：限时选择豁免原用 Prefix 返回 `false` 跳过迭代器方法，会让它返回 `null` 而调用方是 `StartCoroutine(...)`。改为改写 `ref float delay`。

---

## 安装包不再包含安装程序

**改动的理由**：补丁是 BepInEx 运行期挂载的，**不修改任何游戏文件**，
所以「安装」本来就只是把 `mod\package\` 里的东西拷进游戏根目录。
两个 PowerShell 脚本（`安装.ps1` / `卸载.ps1`）带来的复杂度——执行策略、
路径猜测、交互输入、升级/全新/拒绝三分支——和它解决的问题不成比例，
反而多出一层需要测试和维护的代码。删掉。

- `mod\package\` 现在只包含**第三方二进制 + 一份手写说明**，
  唯一自产文件是 `安装说明.txt`
- `安装说明.txt` 全面重写：手动拷贝步骤（含三条容易踩的坑）、当前按键表、
  完整配置项说明、卸载、故障排查、技术说明。之前那份还停在 v0.4.2，
  写着已删除的 `限时选择不自动选`、29 个挂载点、以及「未经实机测试」的告警
- `mod\build.ps1` 相应简化：`$keep` 只剩 `安装说明.txt`，
  结束提示改成「把 package 里的东西全部拷进游戏根目录」
- `.gitignore` 的例外从三个文件减到一个
- `README.md` 的「快速开始」改为手动拷贝说明，并补上第 4 个源文件
  （`Speech.cs` / `UiNav.cs`）、踩坑清单、按键占用

## 提交闸门新增 .ps1 的 BOM 检查

**改动的理由**：Windows PowerShell 5.1 读取**无 BOM** 的 `.ps1` 时按系统
ANSI 代码页（这里是 GBK）解码，中文注释会变乱码并直接语法错误，脚本整个
跑不起来。编辑工具保存 UTF-8 时不写 BOM，`mod\build.ps1` 就这样坏过一次
（表现是 `Unexpected token '}'` 加上一串乱码）。

`tools\check-staged.ps1` 现在除了查版权内容与二进制，还会拦住缺 BOM 的
`.ps1`，并打印补 BOM 的一行命令。已用「去掉 BOM → 拦下 → 恢复 → 通过」
验证过。

---


验收方式：玩家在**试玩版**实机操作 + NVDA 听读，逐项验收通过。
下表只记「已被实机确认」的项。

| 版本 | 验收内容 | 结果 |
|---|---|---|
| v0.5.1 | 非导航模式按空格/回车只推进剧情，不再误触控件 | 通过 |
| v0.5.2 | 限时选择等满 20 秒后自动走沉默分支、剧情继续（不再卡死） | 通过 |
| v0.5.2 | 默认时长 20 秒的体感 | 合适，无需调整 |
| v0.5.3 | 界面变化后重新播报（标题 `START` 弹出二级菜单） | 通过 |
| v0.5.5 | 标题画面 `EXIT` 二次确认；剧情中「返回标题」等只有游戏原生确认框 | 通过 |
| v0.5.6 | 设置界面滑条读出行名 + 百分比（`背景音乐，滑条，60%`） | 通过 |
| v0.5.6 | 设置界面单选项读出行名（`画面模式，全屏模式，开关，开`） | 通过 |
| v0.5.6 | 设置界面页签读出中文（`通用` / `音量` / `快捷键`） | 通过 |
| v0.5.7 | 沉默按键 `0` 立刻沉默并继续剧情 | 通过（v0.5.6 时失效，已修） |
| v0.5.7 | 选项朗读末尾的「按 0 可以立刻沉默」提示 | 通过 |

---

## 后续路线

| 目标 | 说明 |
|---|---|
| `v0.6.0` | 历史回顾（`R` 打开的面板）内容朗读 |
| `v0.7.0` | 画面口述（509 张 CG + Spine 演出） |
| `v0.8.0` | 未配音台词（27,416 行 / 49.3 万字）TTS 预生成，按角色分配音色 |
| `v0.9.0` | 视频口述影像（12 个视频）；全流程无鼠标通关验证 |
| `v1.0.0` | 功能完整、实机验证稳定 |

---

## 提交前安全检查

仓库含 `extracted/`（游戏剧本）与 `decompiled/`（反编译代码）等游戏版权内容，
已由 `.gitignore` 排除。每次提交前请运行：

```powershell
.\tools\check-staged.ps1
```
